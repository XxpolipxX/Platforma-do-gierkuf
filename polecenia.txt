- zbudowanie obrazów
docker-compose build
- uruchomienie kontenerów --detach-mode
docker-compose up -d
- zakmnięcie kontenerów
docker-compose down
- przeglądanie bazy danych  hasło - root
docker run -it --rm --network platforma-do-gierkuf_app_net mysql:8.1 mysql -h db -u root -p gierkowo